import 'package:flutter/material.dart';

class lossers extends StatefulWidget {
  const lossers({Key? key}) : super(key: key);

  @override
  _lossersState createState() => _lossersState();
}

class _lossersState extends State<lossers> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
